# Modeling

Reserved for candidate-job feature generation, learned reranking, and ranking
evaluation. Its input will be the retrieval shortlist rather than the complete
candidate pool. Embedding similarity should be one feature, not the final score.

Planned after the retrieval baseline is stable.
